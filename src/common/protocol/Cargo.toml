[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rand = "0.8"
rayon = "1.8"

bluer = { version = "0.16", features = ["bluetoothd"], default-features = false }
tokio = { version = "1.35", features =["rt-multi-thread"]}
futures = "0.3"

[lib]

[[bin]]
name = "main"

[build-dependencies]
bindgen = "0.69"